- name: Patch turnstile.ts and fix Chrome discovery
  run: |
    # Fix Chrome: chrome-launcher looks for 'google-chrome' or 'chromium-browser', not 'chromium'
    sudo ln -sf /usr/bin/chromium /usr/bin/google-chrome
    sudo ln -sf /usr/bin/chromium /usr/bin/chromium-browser

    # Write the patched turnstile.ts using python3 to avoid heredoc whitespace corruption
    python3 << 'PYEOF'
content = r"""const fs = require('fs');
const path = require('path');

interface TurnstileData {
    domain: string;
    siteKey: string;
    proxy?: {
        username?: string;
        password?: string;
    };
}

async function turnstile({ domain, proxy, siteKey }: TurnstileData, page: any) {
    if (!domain) throw new Error("Missing domain parameter");
    if (!siteKey) throw new Error("Missing siteKey parameter");

    const timeoutMs = (global as any).timeOut || 60000;

    const timeoutPromise = new Promise<never>((_, reject) => {
        setTimeout(() => reject(new Error("Timeout reached after " + timeoutMs + "ms")), timeoutMs);
    });

    try {
        if (proxy?.username && proxy?.password) {
            await page.authenticate({
                username: proxy.username,
                password: proxy.password,
            });
        }

        const htmlContent = `<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body>
<div class="cf-turnstile" data-sitekey="${siteKey}"></div>
<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
</body>
</html>`;

        // Remove default interception set by index.ts getPage() and replace with ours
        page.removeAllListeners("request");
        await page.setRequestInterception(true);

        page.on("request", async (request: any) => {
            const reqUrl = request.url();

            // Block telemetry
            if (reqUrl.includes("challenges.cloudflare.com/reports/")) {
                return request.abort();
            }

            // Serve local api.js for ANY versioned turnstile api.js path
            if (reqUrl.includes("challenges.cloudflare.com/turnstile/v0/b/") && reqUrl.endsWith("/api.js")) {
                const localPath = path.join(__dirname, '../js/api.js');
                try {
                    const body = fs.readFileSync(localPath);
                    return request.respond({
                        status: 200,
                        contentType: 'application/javascript',
                        body: body
                    });
                } catch {
                    return request.continue();
                }
            }

            // Allow all Cloudflare challenge resources
            if (
                reqUrl.includes("challenges.cloudflare.com") ||
                reqUrl.includes("/cdn-cgi/challenge-platform/")
            ) {
                return request.continue();
            }

            // Allow everything else (needed for setContent to work)
            return request.continue();
        });

        // Navigate to domain to establish the correct origin for Turnstile validation
        try {
            await page.goto(domain, { waitUntil: "domcontentloaded", timeout: 20000 });
        } catch {
            // Ignore navigation errors — we overwrite content next
        }

        // Inject our HTML into the established origin
        await page.setContent(htmlContent, { waitUntil: "domcontentloaded" });

        // Poll for the token that Turnstile writes into cf-turnstile-response
        const tokenPromise = (async () => {
            const start = Date.now();
            while (Date.now() - start < timeoutMs) {
                const token = await page.evaluate(() => {
                    const el = document.querySelector(
                        '[name="cf-turnstile-response"], [name="g-recaptcha-response"]'
                    ) as HTMLInputElement | null;
                    return el ? el.value : null;
                }).catch(() => null);

                if (token && token.length > 20) return token;
                await new Promise(r => setTimeout(r, 500));
            }
            throw new Error("Token never appeared in DOM");
        })();

        return await Promise.race([tokenPromise, timeoutPromise]);

    } catch (e) {
        throw e;
    }
}

export = turnstile;
"""

with open("src/lib/ends/turnstile.ts", "w") as f:
    f.write(content)

print("turnstile.ts patched OK")
print(open("src/lib/ends/turnstile.ts").read()[:200])
PYEOF
